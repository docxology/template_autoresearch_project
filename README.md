# template_autoresearch_project

Release artifacts for the [`docxology/template`](https://github.com/docxology/template) `template_autoresearch_project` exemplar.

PDFs and metadata are published via `scripts/publish_project_release.py`; do not treat this repository as manuscript source.

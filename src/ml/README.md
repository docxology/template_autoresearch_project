# ML package

MNIST task config, numpy-only models, bounded candidate selection, and the public `run_bounded_ml_task` entrypoint. Root `ml_*.py` stubs re-export this package for legacy imports.

See [`AGENTS.md`](AGENTS.md) and [`../AGENTS.md`](../AGENTS.md).

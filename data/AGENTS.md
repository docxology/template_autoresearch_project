# Data Notes

This directory may contain small public input fixtures required for deterministic
project tests and analysis. Do not add generated outputs here.

`mnist_small.npz` is a curated MNIST subset used by the default offline
AutoResearch task. If the subset is regenerated, update
`mnist_small_provenance.json`, tests, and the manuscript claims together.
